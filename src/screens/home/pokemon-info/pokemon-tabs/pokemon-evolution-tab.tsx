import React from 'react';
import { ScrollView, Text, View } from 'react-native';

export const PokemonEvolutionTab = () => (
  <ScrollView style={{ flex: 1 }}>
    <Text>Test</Text>
  </ScrollView>
);
